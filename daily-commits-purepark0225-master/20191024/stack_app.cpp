#include <iostream>

#include "my_stack.h"
#include <random>
#include <cstring>

int main()
{
	char buf[100];
	std::cout << "Enter expression:"
	std::cin.getline(buf, 100);

	for(int i = 0; i < strlen(buf); i++)
	{
		
	}
	return 0;
}